package com.zosh.domain;

/**
 * Tax type enum to define how tax is applied
 */
public enum TaxType {
    INCLUSIVE,  // Tax is included in the displayed price
    EXCLUSIVE   // Tax is added on top of the displayed price
}
